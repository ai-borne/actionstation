# ActionStation — Launch Checklist (Single Source of Truth)

> **Goal: the Gold Standard web app for Building a Second Brain (BASB).**
> Every sprint starts by reading this file and ends by ticking boxes in it, with evidence.
> Last verified against live systems: **2026-09-20** (Sprint 2b: payment chain proven end to end in Razorpay test mode)

---

## How to use this document

1. **Sprint start:** read the *Status snapshot* and pick the open items for the sprint. Do not start work that is not listed here — add it first.
2. **During the sprint:** new blockers or discoveries are added immediately, with an ID, a milestone tag and a source.
3. **Sprint end:** tick `[x]`, and append the evidence (date, PR/commit, or "verified in console") to the item. Add a line to the *Change log*.
4. **Never tick from memory.** Tick only after verifying in code, CI, or the live system. Roadmap markdown can be stale; this file must not be.
5. **Owner tags:** `(Claude)` can be done in the repo/CLI. `(You)` needs your account, money, legal sign-off, or a human decision.

**Milestone tags**

| Tag | Milestone | Meaning |
|-----|-----------|---------|
| `M0` | Closed beta | Live on `www.actionstation.in`, invite-only, payments in **test mode**, not promoted |
| `M1` | Public India paid launch | Real payments, monitoring, legal and support in place |
| `M2` | Gold Standard | BASB product excellence, E2E-proven, scale-ready marketing |

---

## Status snapshot (2026-09-20)

| Area | State |
|------|-------|
| 2026-09-24 | **Razorpay guidance applied (branch `feat/razorpay-compliance-pages`, not merged).** Read `razorpayGuildelines` against this checklist. B12 decided (one account). New items B18-B21: dead `/contact` route, missing `/refund` page, webhook 400 on unkeyable events, merchant note at checkout. Contact SSOT now holds phone and Pune address; Terms and Privacy print the same email, phone and address (via `ContactDetailsInline`) and their last-updated date is bumped to 2026-09-24. D1's email-only contact scope is superseded for the Contact page (Razorpay needs phone and address). Verification: root `npm run check` 17,921 tests, functions `npm run check` 580 tests, lint zero warnings. Not yet verified: production behaviour, Razorpay KYC review |
| Code (`main`) | Sprints 1–4, A, C, Sprint 1 (#54, `94ee7ef`) and Sprint 2 (#55, `1b57b3a`) merged and deployed 2026-09-20 (runs 35489583690, 35490056120): 627 test files / 17,133 tests + 522 functions tests pass |
| Production site | `www.actionstation.in` **live on the new build** (deployed 2026-09-20 06:47 IST). Hosting, 24 functions, rules and indexes deployed by CI |
| Domain / DNS | Connected in Firebase Hosting; apex `actionstation.in` → 301 → `www` |
| Payments | Razorpay in **TEST mode** (`rzp_test_`). Stripe secret is a **placeholder** (not launching). **Proven end to end 2026-09-20 (Sprint 2b)**: order → payment → webhook 200 → Pro → refund → Free → delete-with-plan (B2, B4, B6, B7 ticked). Functions pin secret v3 (`razorpaywebhook-00013`, `createrazorpayorder-00013`, `onuserdeleted-00007`); v1/v2 disabled. Open: B16 (late `payment.captured` retry after a refund re-grants Pro), B12 (shared account), B1/B3/B5/B9 (yours) |
| WAF (Cloud Armor) | **Not deployed** (Compute API disabled). Deferred by decision |
| Monitoring alerts | 9 enabled alert policies (+2 uptime policies) and 8 log metrics on channel `Eden Alerts` (email); `webhook_processing_error` alert added 2026-09-20. Fixed for gen2 on 2026-09-20 (were dead). **Email delivery proven (C3e, Sprint 3): the 06:02 UTC `webhook_processing_error` event produced an ALERT email at 06:06 UTC and a RESOLVED email at 06:11 UTC in the inbox** |
| Backups | **Working since 2026-09-20** (had failed with 403 before): daily export to `…-firestore-backups-immutable`, 30-day retention **unlocked**, restore drill passed. PITR off (C4e decision) |
| Uptime monitor | **Live 2026-09-20**: Cloud Monitoring checks on `www.actionstation.in` and `/health` (6 regions, 5 min), CRITICAL email alerts |
| Support mailboxes | **None** (no MX records) — legal pages already promise them |

## Locked decisions

- **Domain:** canonical `https://www.actionstation.in`; apex redirects to it (2026-09-19)
- **Payments:** Razorpay only for launch; Stripe deferred
- **Hosting / storage:** Firebase Hosting + Firebase Storage. Revisit Cloudflare Pages / R2 only when the bill justifies it
- **WAF:** defer Cloud Armor (~$20–30/mo fixed); revisit Cloudflare-in-front vs Cloud Armor before M1 marketing
- **Launch timing:** no urgency; quality over date
- **`actionstation-website` folder:** unrelated AI-BORNE leftover; not part of this launch

---

## A. Release and deploy `M0`

- [x] A1 Domain connected: Firebase Hosting custom domains, Hostinger DNS, Auth authorized domains, OAuth origins/redirects — *2026-09-19*
- [x] A2 SEO files aligned to `www.actionstation.in` + structural test — *`7c02d8b`*
- [x] A3 CI green on PR #51 (gitleaks CLI, audit high/critical cleared, Lighthouse env) — *2026-09-19*
- [x] A4 Merge PR #51 → production deploy — *#51 merged `c56dee1`; first deploy failed at storage rules; #52 fixed it; deploy run 35480556053 succeeded 2026-09-20*
- [x] A5 New functions deployed: `gdprServerExport`, `onStorageObjectFinalized`, `onStorageObjectDeleted` — *`firebase functions:list` shows 24 functions, 2026-09-20*
- [x] A6 Live headers/SEO: canonical, sitemap, CSP includes `challenges.cloudflare.com`; apex 301 → www; `/terms`, `/privacy` 200; `/health` ok — *curl, 2026-09-20*
- [ ] A7 Prod smoke test on `www.actionstation.in`: Google login, Turnstile, save, AI, link preview, upload, calendar connect, export, delete account. **Sprint 4 run 2026-09-20 on the owner's main account (a throwaway account could not sign in: Google asked for its own verification). Proven with evidence:** Turnstile (`verifyturnstile` 200 at 13:25 and 17:23 UTC); save + full reload (a new card persisted); AI (`geminiproxy` 200 at 17:34:46 UTC, model reply on the card; `[SECURITY:ai_prompt] Prompt accepted` audit line); link preview (`fetchlinkmeta` 200 at 17:36:45, card shows the page title); upload (Knowledge Bank image upload created `…/knowledge-bank/…/as-test-upload.jpg` in Storage at 17:37:30 and Gemini described the image; a `.txt` upload also worked); calendar connect/create/update/delete (see A10c, A10d); export (`Export All My Data` and `Export Workspace` both download, counts match the UI, no `data:` URIs). **Still open: delete account** (throwaway account only; never on the owner's main account). Google login itself is only evidenced by the live session
- [x] A10 **Google sign-in shows "Google hasn't verified this app" (found 2026-09-20, Sprint 2b, first sign-in of a throwaway account).** `src/config/firebase.ts:74` adds the *sensitive* scope `calendar.events` to the login `GoogleAuthProvider` and the OAuth app is unverified, so every new user gets a scary warning plus an Advanced → "Go to … (unsafe)" step, and unverified apps are capped at 100 users. Fix options: (a) drop the scope from login and request it only on "Connect Calendar" (a server-side calendar OAuth flow already exists in `calendarAuthService.ts`), (b) complete Google OAuth verification (privacy policy, domain, scope justification). Recommend (a), then (b) if calendar stays a headline feature. Sprint 3 candidate (touches C2/D1/D2) `(Claude for (a); You for (b))`. **Sprint 3 progress (option (a), code done in this PR, not yet ticked):** the scope is removed from the login provider (`src/config/firebase.ts`); tests: `firebase.googleProvider.test.ts` (login provider adds no scopes) and `noSensitiveLoginScope.structural.test.ts` (no `addScope`/sensitive scope in login files; Connect Calendar still requests `calendar.events` offline). Nothing read the sign-in access token, so no behaviour depends on the old grant. **Merged (#63, `57bd977`) and deployed (run 35503713329 succeeded) 2026-09-20.** **Fixed and proven live 2026-09-20 (Sprint 3):** PR #63 (`57bd977`, deploy run 35503713329) removed `calendar.events` from the login provider. The owner signed in from an incognito window with a throwaway Google account (screenshots): Google's sign-in dialog for `actionstation-244f0.firebaseapp.com` lists only *Name and profile picture* and *Email address*, there is no 'Google hasn't verified this app' page, and the app then loaded a new Untitled Workspace. Still visible to users: the dialog names the app by its Firebase domain (not 'Action Station') and its Privacy Policy / Terms links are empty because branding is unverified: A10b
- [ ] A10b Google OAuth verification for the opt-in Connect Calendar scope (still shows the warning and the 100-user cap; if the consent screen is in Testing status, refresh tokens also expire after 7 days). Plan and gaps (Privacy Policy Limited Use statement, token storage/revocation wording, D1 mailboxes, domain verification, demo video): `docs/runbooks/GOOGLE-OAUTH-VERIFICATION.md`. **Consent screen read 2026-09-20 (read-only):** status is **In production**, External (so no 7-day refresh-token expiry), user cap 2 / 100; **no scopes are declared** under Data Access (so the Verification Center says verification is not required, which is misleading), privacy policy / terms / home page links are empty, no logo, branding unverified; authorized domains `actionstation.in`, `…web.app`, `…firebaseapp.com` present. Next steps are in the runbook `(You)` **Progress 2026-09-20 (console changes made by Claude at the owner's request, nothing submitted to Google):** Branding saved (app name `ActionStation`, home page, privacy and terms links, logo = the favicon mark rendered as a 240 px PNG, authorized domains unchanged); `…/auth/calendar.events` declared under Data Access (sensitive, 'approval required'); Verify branding NOT clicked. Code side in PR #68: Calendar grant is revoked at Google on disconnect and account deletion, Privacy Policy section 4 carries the Limited Use statement. **Remaining `(You)`:** **Update (same day, owner: 'use the code-correct choice'):** the declared scope was swapped to the narrower `calendar.events.owned` (we only touch the primary calendar; still sensitive, so verification applies), the code follows in PR #70, and the justification (814/1000 chars) is saved in the console. `founder@ai-borne.in` was added as a developer contact (persisted after reload). The support email stays `mail.sunilpawar@gmail.com`: Google's dropdown only lists the signed-in account and Google Groups it manages, so `founder@ai-borne.in` can only be chosen by signing in to the console as that account. **Remaining `(You)`:** (1) **demo video: pending, cannot be generated by Claude** (a valid demo needs a real Google sign-in and consent screen, and a YouTube upload from the owner); shooting script in `docs/runbooks/GOOGLE-OAUTH-VERIFICATION.md`; paste the link in Data Access; (2) merge and deploy #70 so the app requests the same scope that is declared; (3) verify `actionstation.in` in Google Search Console; (4) optionally switch the support email to `founder@ai-borne.in` (sign in to the console as that account, or D1 `support@`); (5) then Verify branding and submit **Live calendar test 2026-09-20 11:28 UTC (owner): Connect Calendar and create-event worked (`exchangecalendarcode` 200, `calendarcreateevent` 200, event badge on the card) but on the OLD build: the deploy carrying #70 (`calendar.events.owned`) had not finished, so the broader `calendar.events` was requested. The `.owned` scope is still UNPROVEN: repeat after deploy run 35507740596 succeeds, with an account that has not connected before (there is no in-app Disconnect, A10c; otherwise remove ActionStation at myaccount.google.com/permissions and clear `actionstation_calendar_connected` in localStorage), and expect the consent screen to say 'See, create, change, and delete events on Google calendars you own'** **`.owned` consent proven live 2026-09-20 12:2x UTC (owner screenshots):** after Disconnect and Connect, Google's consent page reads 'See, create, change, and delete events on Google calendars you own' (i.e. `calendar.events.owned`), preceded by the expected 'Google hasn't verified this app' screen. The chooser and consent name the app `actionstation.in` and link the Privacy Policy and Terms (branding still unverified, so the name/logo are not shown). **Event creation on this scope proven live** (owner, several times): `calendarCreateEvent` 200 at 12:32:00, 12:48:51, 12:51:00, 13:27:04 and 13:29:07 UTC. One 401 at 12:47:42 UTC was the designed recovery path (`Calendar not connected — REAUTH_REQUIRED` → app auto-disconnect at 12:47:43 → owner reconnected 12:48:15 → create 200), consistent with the grant having been removed at Google. **Edit and delete on the new scope are NOT proven:** no `calendarUpdateEvent` or `calendarDeleteEvent` call has been logged since the reconnect (13:33 UTC). By design of the current code, update only fires from the sync badge's retry (failed/pending events) and delete only when a card that has a synced event is deleted through the card's own delete handler (`useIdeaCardHandlers` → `cleanupOnDelete`), so the owner's edits made no server call: see A10d. **Demo video uploaded 2026-09-20 (unlisted on YouTube; the link is deliberately NOT stored in this public repo). Owner to check before submitting:** it must show the consent page with the `client_id` and the `.owned` scope text, event creation, and Disconnect. If it includes an 'edit the card and the Google event updates' step, that step cannot work today (A10d) **Demo video link entered in the Google console (Data Access → Demo video) 2026-09-20 via Claude in Chrome at the owner's request; persisted after reload; nothing submitted.** Verification Center (read-only): branding 'is not being shown to users'; data access 'not verified'; **Google requires branding to be verified and published BEFORE the data-access verification can be requested**, so the order is: (1) `Verify branding` on the Branding page (needs `actionstation.in` verified in Search Console), (2) then 'Prepare for verification' / submit the scope request **VERIFICATION SUBMITTED 2026-09-20 (via Claude in Chrome, owner's explicit go-ahead): (1)** `actionstation.in` verified in Search Console as a Domain property by a DNS TXT record at Hostinger (**never remove that record**; the other TXT is Firebase's); **(2)** branding verified and published ('verified and is being shown to users'); **(3)** sensitive-scope request for `calendar.events.owned` submitted with the justification, demo video, contacts (`mail.sunilpawar@gmail.com`, `founder@ai-borne.in`) and questionnaire answers (not personal / internal / dev-test / WordPress plugin: all No) plus the two acknowledgements. **Status: 'data access is under review'.** Reviewers may email both contacts and may lock edits meanwhile; users still see the unverified-app screen at Connect Calendar until approval. Tick A10b when Google approves and a fresh account sees no unverified screen
- [x] A10c **No user-facing way to disconnect Google Calendar (found 2026-09-20, Sprint 3).** `disconnectGoogleCalendar()` is only called automatically when the Google session expires (`useCalendarSync`, `calendarIntentHandler`); Settings has no Calendar control. PR #68 made `disconnectCalendar` revoke the grant at Google, but nothing lets a user trigger it, so the demo video and the Privacy Policy cannot show or promise it (policy wording corrected in PR #70 to say account deletion revokes it). Add a Calendar section in Settings (Connected as … / Disconnect) that calls the existing flow, then restore the sentence in `GoogleDataSection.tsx` `(Claude, needs acceptance criteria)` **Built and merged (#72, `a3de80e`; deploy run 35509634674). Verified live 2026-09-20 by the owner (screenshots) and logs:** Settings → Account shows the card; Disconnect flipped it to 'Not connected' with a Connect button (`disconnectcalendar-00018` 200 at 12:23:34 UTC, about 3 s, and no 'Calendar grant revoke' warning logged, i.e. Google accepted the revoke); Connect then showed Google's consent with 'See, create, change, and delete events on Google calendars you own' and the card returned to Connected (`exchangecalendarcode` 200 at 12:30:12 UTC). The definitive Google-side check (ActionStation disappearing from myaccount.google.com/permissions after Disconnect) is step 7 of the demo-video script; the earlier text follows. Settings → Account → Google Calendar card, MVVM: `CalendarConnectionGroup` (View) over `useCalendarConnection` (ViewModel) over `calendarAuthService.disconnectGoogleCalendarConfirmed` (awaits the server, which revokes at Google and deletes the token; on failure stays connected + error toast). Also a Connect button when not connected. The AUTOSAVE yield constant is now shared (`calendar/config/connectTiming.ts`), the auto-disconnect path shares its internals (`clearLocalConnection`, `callDisconnectFunction`), and Privacy Policy section 4 says the user can disconnect in Settings → Account. Tests: service (4), hook (7), view (3), account-section composition (2), policy (5). **Tick after** merge, deploy and a live disconnect: the card flips to Not connected and ActionStation disappears from myaccount.google.com/permissions
- [x] A10d **Editing a card did not update its Google Calendar event (found 2026-09-20, Sprint 3; fixed in #75).** **Verified live 2026-09-20 17:50–17:55 UTC on the owner's account:** three dated cards created (`calendarCreateEvent` 200 ×3), one card edited (`calendarUpdateEvent` 200 at 17:52:50), that card deleted with its own control (`calendarDeleteEvent` 200 at 17:53:34, undo window waited out), the other two deleted together by multi-select (`calendarDeleteEvent` 200 at 17:54:40 and 17:54:41). The Google Calendar connector then showed no events on 21 Sept and the calendar's `updated` stamp was 17:54:42Z. Not exercised live: the toolbar 'clear all' route (it would delete real cards; covered by unit tests only) and the workspace-delete route. See A10e for a related server bug
- [x] A8 **Sign in once before 2026-10-13** — Google deletes the unused OAuth client (`190777323740-ccsb…`) after that date (satisfied by A7). **Sprint 4:** successful production Google sign-ins exist (owner's session; the A10 incognito sign-in), but nothing in the repo or logs says which OAuth client Firebase's Google login uses, so this is not ticked. `(You)` open Google Cloud Console → APIs & Services → Credentials → that client and confirm it no longer shows as unused / has a recent last-used date. **Sprint 4 follow-up (2026-09-20, read-only in the consoles):** Firebase Authentication → Google → Web SDK configuration uses exactly this client (`190777323740-ccsbt…`), and it is the project's only OAuth client, so every production Google sign-in counts as use of it. Google Auth Platform → Clients shows *Last used date: 16 April 2026 (data can lag by a day or more)* and the warning 'must be used before 13 October 2026'. Today's sign-ins and calendar exchanges are not reflected yet. `(You or Claude)` re-check the Last used date on or after 2026-09-22; tick A8 once it moves past 16 April 2026, and if it has not by early October sign in once more (the deadline is 2026-10-13) — *Ticked 2026-09-23: Google Auth Platform → Clients → `190777323740-ccsbt…` showed **Last used date: September 23, 2026** (read in the console during the Google sign-in fix session), well past 16 April 2026; production Google sign-ins in Chrome and Safari the same day*
- [x] A9 No debug flags in the prod bundle: no dev-bypass flag, no embedded App Check debug token, no direct Gemini endpoint, no `rzp_test_`/`sk_` strings — *scanned live JS, 2026-09-20*
- [x] A10e **`calendarDeleteEvent` returns 500 when the Google event is already gone (found 2026-09-20, Sprint 4).** `functions/src/calendarEvents.ts` `gcalFetch` maps every non-OK Google status (including 404/410 for an already-deleted event) to `HttpsError('internal')`; a burst of card deletes on 2026-09-20 at 14:03:55 UTC produced one such 500. Deleting something that is already gone should succeed quietly, otherwise the card shows a failed sync for an event that no longer exists. **Fixed 2026-09-22 (TDD), merged and deployed 2026-09-23:** `gcalFetch` takes an optional `treatAsSuccess` status list; `calendarDeleteEvent` passes `[404, 410]` (an already-gone event now resolves `null`, same as 204), scoped to DELETE only — `calendarCreateEvent`/`calendarUpdateEvent`/`calendarListEvents` pass nothing, so a 404 on those still throws `internal` (a missing event on update is a real error, unlike delete's idempotency). Tests: 4 new cases in `functions/src/__tests__/calendarEvents.test.ts` (404 and 410 on delete → `null`; 500 on delete still throws `internal`; 401 on delete still throws `unauthenticated`; 404 on update still throws `internal`, proving the change is scoped). `functions/npm run check` green (49 files / 558 tests, lint 0, build succeeds). PR #83 merged `c54cb7b`, Production Deploy run 35804012653 succeeded 2026-09-23 00:53 UTC. **Not yet proven live:** an actual double-delete of an already-gone Google event against the deployed `calendarDeleteEvent` (would need a repro on the owner's calendar) — code and CI evidence only

## B. Payments — Razorpay `M1`

- [ ] B1 Start Razorpay **live activation / KYC now** (long lead time) `(You)`
- [x] B2 Test-mode end-to-end: order → payment → webhook → Pro unlocked → AI limit lifted — *Verified live 2026-09-20 (drill steps 2–3, third payment `pay_TeDdOA7N874UH3`): `createRazorpayOrder` "Razorpay order created" 08:05:26 UTC; webhook POST **200** 08:05:36; no `webhook_processing_error`; `subscription/current` = `tier: pro`, `isActive: true`, `provider: razorpay`, `gatewayPlanId: plan_pro_annual_inr`, `lastEventId: pay_TeDdOA7N874UH3`, `expiresAt` = 2027-09-20T08:05:32Z (payment + exactly 365 days); Settings shows Pro/Active. **Caveats:** (1) the app has no visible AI counter, so the 500/day limit is verified in code (`maxAiGenerationsPerDay: 500`, enforced server-side via `resolveEffectiveTier`), not in the UI; (2) the replay step "already processed" could not be run live because this Razorpay dashboard has no resend button (unit-tested)*
- [x] B2a **Payer attribution (found 2026-09-20; corrected same day):** `handlePaymentCaptured` read the payer from `payment.notes`, which the client controls, so a payer could not be trusted. Fix: resolve `userId`/plan from the server-set order (`orders.fetch`) and ignore payment notes; a genuine ActionStation payment that cannot be attributed is one `webhook_processing_error` + 200. *Correction:* an earlier version of this item claimed this was the root cause of C3c. It was not: see C3c and B12 — PR #55, CI run 35489003654 green; 12 handler tests. Live proof: drill step 3
- [x] B2c **Revenue hole (found 2026-09-20):** `createRazorpayOrder` accepts the ₹100 test monthly plan (and placeholder USD plans), while `handlePaymentCaptured` grants 365 days of Pro for any captured payment. Worse, the client's `PRO_ANNUAL_PLAN_ID` is `plan_SWtIj1spzXCZbR`, i.e. the ₹100 plan, so every real checkout from the UI creates a ₹100 order and unlocks a year. Fix: the client sends the real annual plan id; only the annual INR plan is orderable; the webhook rejects a payment below the plan price — *fixed in PR #55 (stacked on #54), CI run 35489003654 green: `getOrderAmount` only prices the annual INR plan (monthly ₹100, USD, unknown ids get 400, tested in `createRazorpayOrder.test.ts`); client now sends `plan_pro_annual_inr` (was the ₹100 plan id); webhook rejects a plan/amount mismatch. **Live proof 2026-09-20 06:00:54 UTC:** a browser still running the pre-deploy bundle sent the ₹100 plan id and `createRazorpayOrder` answered 400*
- [x] B2d **Pro never expires server-side (found 2026-09-20):** `geminiProxy` treats `tier === 'pro'` as Pro forever and ignores `expiresAt`/`isActive`; only the client downgrades an expired plan. Fix: one server-side effective-tier helper — *fixed in PR #55 (stacked on #54), CI run 35489003654 green: `effectiveTier.ts` (expired or `isActive:false` = free) used by `geminiProxy`; 6 unit + 2 proxy tests*
- [x] B2b **Price mismatch (found 2026-09-20):** UI and landing say ₹2,999/yr, `createRazorpayOrder` charges ₹4,999 (`499900` paise); monthly INR plan is a ₹100 test amount. Decision: ₹2,999/yr. The server price becomes the SSOT and the copy derives from it — *fixed in PR #55 (stacked on #54), CI run 35489003654 green: server `PRO_ANNUAL_INR_PAISE = 299_900`; client `PRO_ANNUAL_PRICE_INR`/label derived in `types/pricing.ts`; `razorpayPriceSync.structural.test.ts` keeps client and server equal*
- [x] B6a `refund.processed` is not handled, so a refunded user stays Pro. Policy decided 2026-09-20: 7-day full refund, issued from the Razorpay dashboard; the webhook downgrades automatically — *fixed in PR #55 (stacked on #54), CI run 35489003654 green: `refund.processed` handled; full refund of the current payment downgrades (`downgradeToFreeIfCurrentPayment`, transactional), partial refund keeps Pro, older-payment refund is ignored. Live proof: drill step 5*
- [x] B7a Account deletion with an active annual plan only logs "manual refund review" and then deletes the subscription doc, losing the payment trail. Decision 2026-09-20: warn in the confirm dialog, delete, keep a minimal server-only payment record — *fixed in PR #55 (stacked on #54), CI run 35489003654 green: `paymentRecordWriter.ts` writes server-only `paymentRecords/{paymentId}` before erasure, deletion aborts if that write fails; `DangerZone.tsx` warns Pro users; Terms §5/§7 updated. Live proof: drill step 7*
- [ ] B3 Replace `RAZORPAY_KEY_ID` / `RAZORPAY_KEY_SECRET` with **live** keys in Secret Manager `(You)`
- [x] B4 (test mode) Webhook registered at `razorpayWebhook` URL; `RAZORPAY_WEBHOOK_SECRET` matches — ***Delivery proof 2026-09-20:** after the B15 rotation, real deliveries return 200 (08:05:36 `payment.captured`; 08:08:53, 08:11:04, 08:24:37 `refund.processed`). **The live-mode webhook does not exist yet and stays part of B1/B3 (Runbook 5).*** *Earlier verification 2026-09-20: service `razorpaywebhook` public invoker; `RAZORPAY_WEBHOOK_SECRET` pinned to version 1 (only version). Razorpay dashboard, **test mode**: webhook `https://us-central1-actionstation-244f0.cloudfunctions.net/razorpayWebhook` Enabled, secret set; `refund.processed` **added 2026-09-20 10:52 IST** (was missing; now 7 events: `payment.captured`, `refund.processed`, 5× `subscription.*`), read back after reload; the URL reaches our function (GET → 405 from our handler). The dashboard is in Live view with no activation, so **no live webhook exists yet** (B1/B3). **Open:** a real delivery returning 200 proves the secret matches (drill); at live switch register a separate live webhook (Runbook 5)*
- [ ] B5 Real small live payment + refund drill
- [x] B6 Cancel / refund flow for the annual model works without the Stripe portal — *Verified live 2026-09-20 (test mode): full refund of `pay_TeDdOA7N874UH3` from the Razorpay dashboard → `refund.processed` webhook **200** 08:08:53 UTC, log `Full refund — downgraded to free` 08:08:55, `subscription/current` = `tier: free`, `isActive: false`, app shows Free. Older payment `pay_TeCldxlLlkYX88` refunded 13:40 IST → 200 + `payment no longer current, tier unchanged` (subscription untouched). Not covered live: the partial-refund case (unit-tested only). One refund failed: `pay_TeCo0SIEDxJ29q` (12:46 IST) is still captured and unrefunded, because the Razorpay dashboard answered "invalid request sent" twice (same amount and method as a payment that refunded fine): see B17*
- [x] B7 Delete-account with active Pro flags/cancels correctly (plan scenario 8) — *Verified live 2026-09-20 on a throwaway Google account (not the owner's), payment `pay_TeDtPj9c30MbmD`: Pro granted (webhook 200 08:20:47 UTC); the confirm dialog showed "active Pro plan… does not refund it automatically"; after Delete: `paymentRecords/pay_TeDtPj9c30MbmD` exists (`reason: account_deleted_with_active_plan`, plan, currency, expiresAt, uid); `users/{uid}` subscription/workspaces/knowledgeBank/usage gone (404 / 0 docs); Auth user gone; Storage `users/{uid}/` empty; logs `payment record retained for refund review` and `account_deleted` at 08:22:34–35 UTC; an unauthenticated read of the record returned 403 PERMISSION_DENIED (no explicit rule, deny-by-default; not tested as a different signed-in user). Refund afterwards: `refund.processed` **200** 08:24:37, `payment no longer current, tier unchanged`, no `webhook_processing_error`*
- [x] B8 Verify no Stripe UI path is reachable in production (secret is a placeholder) — *Client Stripe code removed (`useCheckout`, `useBillingPortal`, portal branch, unused strings) and `noStripeUi.structural.test.ts` added; a legacy Stripe-provider Pro user gets support copy instead of a portal. Server callables stay deployed (placeholder secret, no client path). Verified live 2026-09-20 after deploy run 35490056120: crawled all 25 JS chunks of `www.actionstation.in`: 0 references to `createCheckoutSession`, `createBillingPortalSession` or `billing.stripe.com`*
- [ ] B9 GST / invoicing approach decided (Razorpay dashboard invoices vs automation) `(You)`
- [x] B10 Payment runbook (`docs/runbooks/PAYMENT-INCIDENTS.md`) is Stripe-centric — add Razorpay procedures — *`docs/runbooks/PAYMENT-INCIDENTS.md` rewritten for Razorpay (v2.0, 6 runbooks incl. refund, live-key switch and the pinned-secret-version gotcha) and `PAYMENT-E2E-DRILL.md` added; log lines and alert names checked against the live project, 2026-09-20*
- [x] B11 Pricing, Terms and FAQ copy match actual tier limits (`tierLimits.ts` is the SSOT) — *PR #55 (stacked on #54), CI run 35489003654 green: landing pricing derives from `tierLimits.ts` (Free 5/12/60/50 MB, Pro 50/500/500/5120 MB) and `types/pricing.ts`; upgrade CTA, landing price, FAQ, Settings and Terms all state ₹2,999/year, one-time, 7-day refund. Live 2026-09-20: bundle contains `plan_pro_annual_inr`, `2999` and the refund copy, and no `plan_SWtIj1spzXCZbR`*
- [ ] B12 **Razorpay account is shared with SSBMax (found 2026-09-20).** Every event of either product is delivered to both webhooks (ours receives SSBMax's customer email/phone in payloads; SSBMax's `handleRazorpayWebhook`, 12 events, receives ours). Code side done in PR #58: ActionStation ignores anything not stamped `notes.source = actionstation` (payments via order, and `subscription.*`). **Decide before going live** `(You)`: (a) keep one account and accept cross-delivery, or (b) a separate Razorpay account for ActionStation (cleaner: own KYC/settlements/GST, no shared payloads). Sprint 2b addition: the API key secret shown in an earlier chat is still valid and is the key SSBMax also uses, so it cannot be rotated without coordinating with SSBMax. Either way remove the unused `subscription.*` events from our webhook (ActionStation sells one-time orders only) and tell the SSBMax owner their webhook receives our events — ***Decision 2026-09-24 (owner, from Razorpay guidance in `razorpayGuildelines`): option (a), one account for ai-borne.in / ssbmax.in / actionstation.in, single personal bank account, display name `ai-borne`.** Code side: checkout now says "Payments securely processed by ai-borne via Razorpay" (B21). Still open `(You)`: remove the unused `subscription.*` events from the ActionStation webhook in the Razorpay dashboard; make sure SSBMax filters on its own `notes` and returns 200 for ours; tell the SSBMax owner (the same person) that live keys are shared, so B3 rotates both apps together*
- [x] B13 **ActionStation checkout is broken: Razorpay rejects our test API key (found 2026-09-20 during the drill).** `createRazorpayOrder` fails with `BAD_REQUEST_ERROR: Authentication failed` (Cloud Logging 2026-09-20 06:02:22 UTC, revision `createrazorpayorder-00012`). Secret Manager `RAZORPAY_KEY_ID` v1 = v2 = `rzp_test_SWt9…` (March). The Razorpay dashboard (test mode) lists a single active key, `rzp_test_TRFC…`, **generated 18 Aug 2026**; regenerating a key retires the old one, and 18 Aug is the day SSBMax's first test payments appear, so the shared account's key was regenerated for SSBMax. Upgrading has therefore failed for everyone since 18 Aug, and it never showed because nothing alerted on it (the new `webhook_processing_error` alert now does). Fix `(You)`: put the *current* test key id and secret into Secret Manager (`RAZORPAY_KEY_ID`, `RAZORPAY_KEY_SECRET`; the secret is only shown at generation, so take it from SSBMax's config) and redeploy the three functions that pin them (Runbook 5). **Never click Regenerate Key** — that would break SSBMax. This is the concrete case for B12: separate accounts, or accept that one product's key change breaks the other — *Fixed 2026-09-20: current key added as `RAZORPAY_KEY_ID` v3 / `RAZORPAY_KEY_SECRET` v3 (key id read back = `rzp_test_TRFC…`, secret length 24) and `RAZORPAY_WEBHOOK_SECRET` v2; manual deploy run 35493746803 succeeded; `createrazorpayorder-00013`, `razorpaywebhook-00012`, `onuserdeleted-00007` pin the new versions. Live proof 06:24:59 UTC: `POST createRazorpayOrder` 200 + "Razorpay order created". The old key versions are retired and still enabled: disable them after the drill (Runbook 5 step 7)*
- [x] B14 **Razorpay Checkout cannot open in production: CSP `frame-src` lacks `https://api.razorpay.com` (found 2026-09-20 in the drill, right after B13).** Standard Checkout renders its payment page in `iframe.razorpay-checkout-frame` with `src=https://api.razorpay.com/v1/checkout/public`; `firebase.json` allows only `https://checkout.razorpay.com` in `frame-src`, so the browser shows a blank "This content is blocked" overlay. `cspCompleteness.structural.test.ts` checks Razorpay only under `connect-src`, so nothing caught it. Fix: add `https://api.razorpay.com` to `frame-src` (CSP lives only in `firebase.json`) plus a new structural test. Live proof needs the hosting deploy and the drill (a payment window that opens) — *Fixed 2026-09-20: `https://api.razorpay.com` added to `frame-src` (PR #60, deploy run 35495722592). Live proof: in the signed-in browser the Razorpay window opened, "ActionStation", Price Summary ₹2,999, red Test Mode ribbon; the `frame-src` violation is gone. Note: a tab that had the old service worker kept the OLD CSP (cached with the shell) until its service worker was cleared; returning users get the fix when they accept "Update now"*
- [x] B14a Two more Razorpay hosts were blocked in the same drill: `https://cdn.razorpay.com` (`script-src`, checkout's risk-detection bundle) and `https://lumberjack.razorpay.com` (`connect-src`, checkout telemetry). Neither stopped payment. Allowed both in `firebase.json`, guarded by the extended `cspRazorpayCheckout.structural.test.ts`. lumberjack is Razorpay's own checkout telemetry; drop it from `connect-src` if you prefer to keep it blocked — *PR #61 merged; deploy run 35497763588 succeeded (attempt 2, see C14)*
- [x] B14b Stale CSP for returning users: the service worker caches the page shell with its headers and only re-checks on navigation, so a tab left open never showed the update prompt. `swUpdateScheduler.ts` now re-checks hourly and when the tab becomes visible (hook wired, 4+2 tests) — *PR #61 merged; deploy run 35497763588 succeeded (attempt 2, see C14)*
- [x] B15 **Webhook deliveries are rejected: the dashboard's webhook secret does not match Secret Manager (found 2026-09-20 in the drill).** Two test payments of ₹2,999.00 were captured at 12:44 and 12:46 IST (amount correct), but every delivery to `razorpayWebhook` returned `400 Invalid webhook signature`, so no Pro was granted and no subscription document exists. Self-signing a test event with the stored secret returned 200 (and a wrong signature 400), so our verification is correct: the value set in the Razorpay dashboard differs from the one stored. Fix: rotate both sides to one new random value (Secret Manager version + the dashboard's "Change Secret") and redeploy, then let Razorpay retry the two deliveries `(needs your yes; you paste the value into the dashboard)` — *Fixed 2026-09-20: a new random value stored as `RAZORPAY_WEBHOOK_SECRET` v3 (length 64, never printed), placed on the clipboard with `pbcopy`; you pasted it via Change Secret in the Razorpay test-mode webhook (dashboard shows Last Updated 01:15 PM); #61 merged → deploy run 35497763588 (first attempt failed on the flaky test C14, attempt 2 succeeded); `razorpaywebhook-00013` pins `RAZORPAY_WEBHOOK_SECRET` key `'3'` (checked in `gcloud run services describe`). Razorpay's own retries of the two old deliveries had failed before the deploy and none arrived after it, so the chain was proven with a third payment. The old v1/v2 versions of all three Razorpay secrets are now **disabled** (v3 only). Exposure note: the previous webhook secret (v2) appeared in an earlier chat and is now disabled; the API key secret shown earlier in chat is still the live shared key (see B12)*
- [x] B16 **A late `payment.captured` retry after a refund re-grants Pro (found 2026-09-20).** `handlePaymentCaptured` never checks whether the payment was already refunded. Razorpay retries failed deliveries for up to 24 h; if a retry of a captured event lands after `refund.processed` (a webhook outage longer than the refund delay, as with B15: two payments were refunded while their `payment.captured` deliveries were still pending), the user gets 365 days of Pro for a refunded payment. Fix: on `payment.captured`, also fetch the payment (`payments.fetch`) and skip when `status` is `refunded` or `amount_refunded > 0`; add a test; then confirm the owner's account was not silently re-granted `(Claude, code + PR)`. **Fixed 2026-09-20 (Sprint 3): PR #64, CI green, merged `d0b6675`, deploy run 35504140246 succeeded; live `razorpaywebhook-00014` (was 00013) still pins all three Razorpay secrets at version 3.** `handlePaymentCaptured` now fetches the payment (`isFullyRefunded`) and skips a fully refunded one (`status: refunded` or `amount_refunded >= amount`; info log, no alert, 200). Deliberate deviation from the wording above: a *partial* refund still grants Pro, matching the B6a policy. A fetch failure throws, so Razorpay retries and nothing is granted unchecked. Tests: `razorpayPaymentHandlers.lateCapture.test.ts` (6). **Owner-account check (2026-09-20, `razorpaywebhook` logs):** no `payment.captured` 200 after the 08:08:53 refund except the throwaway-account payment (08:20:47), so nothing re-granted Pro. Razorpay's retries of the two original drill deliveries (07:14–08:42 UTC, `Razorpay-Webhook/v1`) still return 400 `Invalid webhook signature` because they carry the old signature, so they can never reach the handler; expect them to stop within 24 h (they also trip the `webhook_sig_failure` alert)
- [ ] B17 One test refund is stuck: `pay_TeCo0SIEDxJ29q` (₹2,999, 12:46 IST) is still captured; the Razorpay test-mode dashboard rejects its full refund with "invalid request sent" (tried twice). Test money only. Retry later or raise it with Razorpay support; if it is refunded later the webhook must answer 200 and change nothing (its payment is not `lastEventId`) `(You)`
- [ ] B18 **`/contact` was a dead link (found 2026-09-24, from the Razorpay guidance review).** The landing footer linked `/contact` but `App.tsx` had no such route. Razorpay activation requires a Contact page with email, phone and address. — *Code done on `feat/razorpay-compliance-pages` (not merged): `ContactPage` at `/contact`, details from the `src/config/contact.ts` SSOT (email, `+91 89369 95020`, Pune virtual-office address), `legalRoutes.tsx` resolver routes all four legal pages before the auth-loading guard. 17,921 root tests green. Open: merge, deploy, then open `/contact` signed out on production* `(Claude)`
- [ ] B19 **No standalone Refund & Cancellation page (found 2026-09-24).** Razorpay wants it as its own page linked in every site footer; the policy lived only in Terms §5 and the FAQ. — *Code done on the same branch: `/refund` (`RefundPolicy`), all numbers from `pricing.ts` (`REFUND_WINDOW_DAYS`, `PRO_ANNUAL_PRICE_LABEL`); footer links Terms, Privacy, Refund, Contact; sitemap lists both new pages. Open: merge, deploy, verify signed out* `(Claude)`
- [ ] B20 **Signed webhook with no payment/refund/subscription entity returned 400 (found 2026-09-24).** On the shared Razorpay account (SSBMax), such an event (for example a payout event) made Razorpay retry and could eventually disable our webhook. — *Fixed on the same branch (TDD): it is acknowledged with 200 `ignored: no entity id`, nothing is claimed or handled; `razorpayWebhook.test.ts` updated (14 tests) and functions suite 580 green. Signature failures and malformed JSON still return 400. Open: deploy `razorpayWebhook`* `(Claude)`
- [ ] B21 **Checkout should name the merchant (2026-09-24).** Shared account means customers see `ai-borne` on statements. — *Code done on the same branch: `SubscriptionBillingGroup` shows "Payments securely processed by ai-borne via Razorpay. Prices are in INR." with a link to `/refund` above checkout. Open `(You)`: set the Razorpay display name to `ai-borne`; PAN name must match the bank account name exactly; confirm every submitted site (ai-borne.in, ssbmax.in) has the same four legal links in its footer, since only actionstation.in lives in this repo*

## C. Security and platform `M1`

- [x] C1 App Check **enforcement** on Firestore, Storage, Functions — only after prod clients are verified to send tokens; reCAPTCHA key covers `www.actionstation.in` `(You)` console — *Verified 2026-09-20 (Sprint 3, read-only): Firebase App Check API shows `ENFORCED` for `firestore`, `firebasestorage` and `identitytoolkit` (Auth). Cloud Audit Log: the owner switched them from UNENFORCED to ENFORCED on **2026-03-29 10:23–10:24 UTC** (Storage, Firestore, Auth), so it is not new. Working under enforcement on `www.actionstation.in`: every Sprint 2b drill (sign-in, saves, order, payment) ran with it on, which also shows the reCAPTCHA key accepts that domain. Functions: every app-facing callable/HTTP function enforces in code (`enforceAppCheck: true` on `onCall`, `verifyAppCheckToken` on `geminiProxy`, `fetchLinkMeta`, `createRazorpayOrder`, `proxyImage`, `createCheckoutSession`, `createBillingPortalSession`); `health`, both payment webhooks and `verifyTurnstile` are deliberately open (called by Razorpay/Stripe, uptime checks and the pre-login page). Not enforceable at platform level for gen2 HTTP functions, so this is code-level by design. E2/E3 stay open: they need live proof*
- [x] C2 Turnstile widget hostname allowlist includes `www.actionstation.in`; login smoke test passes `(You)` Cloudflare dashboard — *Verified 2026-09-20 (Sprint 3, read-only, signed in by the owner): the widget **ActionStation Prod** lives in the `mail.sunilpawar@gmail.com` Cloudflare account (the `founder@ai-borne.in` account has none), key `0x4AAAAAADMX6dH9…` = the key in the live bundle, mode Invisible, hostnames `actionstation.in`, `www.actionstation.in`, `localhost`. Last 24 h on `www.actionstation.in`: 5 challenges, 5 solved, 5 siteverify, 5 valid / 0 invalid tokens, matching our 5 × 200. **Hygiene:** `localhost` is on the production widget, so any page served from localhost can obtain valid tokens; recommend removing it and using Cloudflare's dummy test keys for local dev `(You)`. Earlier partial notes: the site key (`0x4AAAAAADMX6…`, public) is in the live bundle; `TURNSTILE_SECRET` has 2 enabled versions (2026-03-18, 2026-05-10); CI passes `VITE_TURNSTILE_SITE_KEY` to build/deploy/preview/lighthouse; real sign-ins verify (see E6). **Open:** read the widget's hostname allowlist in the Cloudflare dashboard (needs your login; I only need to see the list, no change)* **Hygiene done 2026-09-20 (owner request, via Claude in Chrome):** removed `localhost` from the ActionStation Prod widget (Cloudflare: 'Successfully updated site settings'); hostnames are now `actionstation.in` and `www.actionstation.in`, mode still Invisible. Local dev is unaffected: with `VITE_TURNSTILE_SITE_KEY` empty `useTurnstile` skips the challenge (and `.env.local` has none). `.env.example` now says how to use Cloudflare's dummy test keys
- [x] C3 Monitoring: `scripts/setup-monitoring-alerts.sh` run; `auth_failure_spike` and `bot_detected_spike` metrics + policies exist — *2026-09-20: 6 log metrics and 7 enabled policies verified via `gcloud … list`, all on channel `Eden Alerts` (mail.sunilpawar@gmail.com). Chat channel (Slack) not created: no webhook supplied, script supports `SLACK_WEBHOOK_URL`. Email delivery proven by a real alert on 2026-09-20 (C3e: ALERT 06:06 UTC and RESOLVED 06:11 UTC in the inbox)*
- [x] C3e **Alert email delivery (raised 2026-09-20, Sprint 2b as "never arrived"; resolved 2026-09-20, Sprint 3: it did arrive).** The channel `Eden Alerts` (email, enabled, no `UNVERIFIED` flag) delivered both notifications for the `HIGH: Webhook Processing Error` incident: `[ALERT - Error] Payment webhook handler errors…` at 06:06:33 UTC and `[RESOLVED - Error]` at 06:11:30 UTC (duration 4 min 57 s), from `alerting-noreply@google.com` to `mail.sunilpawar@gmail.com`, both in the Gmail **inbox** (labelled Important). Evidence chain: log entry 06:02:23 (`createrazorpayorder`, `webhook_processing_error`) → log metric point `1` at 06:02:41 → policy (created 05:03 UTC, threshold >0 per 5 min) opened the incident → email ~4 min after the event (metric ingestion + 5 min alignment window). The earlier "no email" report was a miss on the receiving side, not a delivery failure; nothing in production changed. All policies share this one channel, so delivery is proven for the whole set. Tip: search Gmail for `from:alerting-noreply@google.com`; the subject starts with `[ALERT - …]`. Not yet exercised: a CRITICAL uptime alert (same channel, different policy type)
- [x] C3a **Dead monitoring (found 2026-09-20):** all 24 functions are gen2 (`resource.type="cloud_run_revision"`), but the two live alerts and the `geminiProxy_429` metric filter on `cloud_function`, so they can never fire. `setup-monitoring-alerts.sh` also uses `resource.type="global"` for the auth/bot alerts (never matches a log-based metric on a Cloud Run log), omits `notificationChannels` on those two policies, is not idempotent (re-run duplicates policies) and hides errors with `2>/dev/null`. Fix the script, then verify each alert against a real matching series — *fixed 2026-09-20: gen2 filters, `ALIGN_DELTA` per-minute counts (old `ALIGN_RATE` with threshold 50 meant 50/sec), upsert by displayName, channel on every policy, errors visible, `mktemp`; the live 2 policies were updated in place. Filter shape confirmed against 31 real `webhook_processing_error` entries*
- [x] C3b gcloud default project on this machine is `payslip-app-475e1`; scripts must always pass `--project` explicitly (they do; keep it that way) — *verified in script, 2026-09-20*
- [x] C3c Razorpay test payments 2026-08-28/29 produced 31 `webhook_processing_error` events (`payment.captured: missing userId in notes`) — *Root cause found 2026-09-20 in the Razorpay dashboard: those were **SSBMax** payments, not ActionStation's. The Razorpay account is shared with another product (GCP project `ssbmax-49e68`, own webhook `handleRazorpayWebhook`) and Razorpay delivers every account payment to every registered webhook. Evidence: `pay_TV84pObzRuEtIs` "SSBMax Pro Membership Subscription" ₹499 with an order and empty notes; all 8 test payments are ₹499/₹999 (ours are ₹100/₹2,999); payment times match the error bursts (08:48–09:11 UTC, 28 Aug). No ActionStation-priced payment has ever been made in test mode, so the checkout is untested end to end (B2). Fixed by ignoring events whose order/subscription lacks `notes.source = actionstation` (info log, 200, no alert) — PR #58*
- [x] C3d **No alert on `webhook_processing_error` (found 2026-09-20):** the 31 C3c failures raised nothing because no log metric or policy matches that event (the 8 policies cover 5xx rate, signature failures, auth, bots, 429s, backup, uptime and `payment_failed`). Fix: `webhook_processing_error` metric + `HIGH: Webhook Processing Error` policy in `setup-monitoring-alerts.sh`; then run the script against production `(needs your yes)` and confirm the policy exists — *Applied to production 2026-09-20 (`setup-monitoring-alerts.sh`, exit 0): metric `webhook_processing_error` created, policy `HIGH: Webhook Processing Error` created (enabled, severity ERROR, channel `Eden Alerts`, threshold >0 per 5 min); the other 9 policies updated in place, no duplicates. The metric filter matches exactly the 31 historical C3c entries (`gcloud logging read`, 90 days). Email delivery is still unproven until a real alert fires*
- [ ] C4 Backups: confirm bucket is immutable (retention lock) per `scripts/setup-immutable-backups.sh`; verify a scheduled backup ran; **restore drill** once — *2026-09-20: new bucket `…-firestore-backups-immutable` (us-central1, uniform access, versioning, 30-day retention, **unlocked**); manual run wrote 338 docs / 925 KB; restore drill passed (see C4f). Left open until the retention lock (C4g) is applied. Legacy `…-firestore-backups` bucket is empty (see C4h)*
- [x] C4a **Backups have never worked (found 2026-09-20):** `firestoreBackup` fires daily 02:00 UTC and fails `403 PERMISSION_DENIED` (Cloud Scheduler status 13). Cause: the gen2 function runs as the default compute SA (`190777323740-compute@…`, only `roles/editor`); `datastore.importExportAdmin` was granted to the appspot SA instead — *fixed 2026-09-20: `roles/datastore.importExportAdmin` granted to the compute SA by `setup-immutable-backups.sh`; scheduler job re-run → export operation SUCCESSFUL*
- [x] C4b Deployed code targets `gs://actionstation-244f0-firestore-backups-immutable`, which **does not exist**. The legacy bucket is empty (0 objects), has no retention policy, no versioning, uniform access off, and a 30-day delete lifecycle — *fixed 2026-09-20: bucket created and verified with `gcloud storage buckets describe`; objects present under `2026-09-20/`*
- [x] C4c `scripts/setup-immutable-backups.sh` is broken for macOS (`${CONFIRM,,}` needs bash 4), grants the wrong SA, swallows errors, uses `/tmp` and deprecated `gsutil`; rewrite it so locking is a separate, explicit step — *rewritten 2026-09-20: idempotent, `gcloud storage`, `mktemp`, correct SAs, no lock step (lock command printed only). Stale header comment in `firestoreBackup.ts` fixed*
- [x] C4d No alert exists for a failed backup — it failed silently at least once (the >50/min 5xx alert cannot catch a once-a-day job). Add a `firestore_backup_failed` log metric + alert — *2026-09-20: `firestore_backup_failed` metric + policy `HIGH: Firestore Backup Failed` created via `setup-monitoring-alerts.sh`*
- [ ] C4e Firestore PITR is **disabled** (version retention 1 h) and there is no managed backup schedule. Decide: enable PITR (7-day window, extra storage cost) and/or a managed daily schedule as a second layer `(You)` decision
- [ ] C4g **Lock the retention policy** on `…-firestore-backups-immutable` — IRREVERSIBLE; recommended only after 2–3 scheduled nightly backups have succeeded. Command in `docs/runbooks/FIRESTORE-RESTORE.md` `(You)` decision
- [ ] C4h Delete the empty legacy bucket `actionstation-244f0-firestore-backups` (or leave; it costs nothing) `(You)`
- [x] C4f Restore drill runbook `docs/runbooks/FIRESTORE-RESTORE.md` — restore into a scratch database, never into `(default)` — *2026-09-20: runbook added; drill restored into scratch DB `restore-drill`, counts matched `(default)` (workspaces 21, nodes 185, edges 84, knowledgeBank 33, usage 1); scratch DB deleted*
- [x] C5 Uptime monitor on `…/health` and on `https://www.actionstation.in` with email alerting (`docs/UPTIME-MONITORING.md`) — *2026-09-20: 2 Cloud Monitoring uptime checks (5 min, 10 s timeout) + 2 CRITICAL alert policies on `Eden Alerts`; `scripts/setup-uptime-checks.sh` idempotent (re-run creates no duplicates). Verified via Monitoring API: `check_passed=true` and HTTP 200 from all 6 regions for the site; `/health` passing from 5 regions incl. the `"status":"ok"` matcher. Email delivery of a real alert on the same channel is proven (C3e); an uptime incident itself has not fired yet*
- [ ] C6 WAF decision recorded: Cloud Armor vs Cloudflare in front vs none for M1 (see Locked decisions)
- [ ] C7 Dependency audit: moderates remain (vitest 5 for dev tooling; `firebase-admin` 14 via `uuid`). Schedule upgrades; CLAUDE.md says audit 0 — *Sprint 3 measurement 2026-09-20 (`npm audit`): root 2 moderate, functions 10 moderate, 0 high/critical (CI's audit gate passes at high). Nothing changed; upgrades still to schedule*
- [ ] C8 CSP `img-src` contains `data:` but CLAUDE.md forbids it — fix or record the exception — *Sprint 3 finding 2026-09-20: live `img-src 'self' data: blob: https:`. Not a one-line fix: `imageInsertService.ts` (and `imageExtension.ts`, which accepts `data:image/`) shows a picked/pasted image inline as a `data:` URL before upload, so removing `data:` would break that path. Options: (a) switch inline preview to `blob:` object URLs (already allowed) and drop `data:`, or (b) record the exception in CLAUDE.md. Recommend (a) as its own TDD PR once you confirm `(Claude, needs acceptance criteria)`*
- [ ] C9 Secret hygiene per `docs/security/KEY-LIFECYCLE.md`; rotation dates recorded
- [x] C14 **Flaky unit test blocks production deploys (found 2026-09-20).** `firebaseUtils.retry.test.ts` ("uses exponential backoff", line 124) spies on the global `setTimeout` and keeps every timer >= 200 ms, so an unrelated 30 s timer in the process can land in the list (`expected 30000 to be 400`). It failed the first attempt of deploy run 35497763588; re-running the failed job passed. Make the spy count only the retry timers (or use fake timers) instead of a magnitude filter `(Claude, small PR)`. **Fixed 2026-09-20 (Sprint 3): PR #65, CI green, merged `45b407f`, deploy run 35504638978 succeeded first attempt.** The test no longer spies on `setTimeout`; it advances the fake clock and asserts the call count at t=199/200/599/600 ms, so unrelated timers cannot affect it. Mutation-checked (a constant delay fails it) and 15/15 repeated runs green
- [x] C15 **Production AI runs on a preview model (found 2026-09-20, Sprint 3).** `GEMINI_MODEL = 'gemini-3.1-flash-lite-preview'` (`functions/src/utils/securityConstants.ts:114`). Preview models can change behaviour or be retired on short notice, so every AI feature depends on it. Also the same literal is duplicated in the dev-only direct client (`src/features/knowledgeBank/services/geminiClient.ts:138`), a second place to change. Decide: stay on the preview, or move to a stable model id and keep one shared constant; add an alert or a smoke test for model errors `(You)` decision, then `(Claude)` for the constant and test — *Owner decision 2026-09-20: the preview id is deprecated, move to the stable `gemini-3.1-flash-lite`. PR #67: server `GEMINI_MODEL` and the dev-only client share the id, and `geminiModelSync.structural.test.ts` keeps them equal and rejects any `gemini-*-preview` id. *Fixed and proven live 2026-09-20: #67 merged (`6ad9ab1`), deploy run 35507391328 succeeded, `geminiproxy-00018` created 11:25 UTC; after the owner ran a generation, that revision returned 200 at 11:27:39 UTC (the calendar-intent prompt 'Schedule my meeting tomorrow at 10am', which uses Gemini, also worked). Model: `gemini-3.1-flash-lite`*
- [ ] C16 **A later merge cancelled the earlier merge commit's CI on `main` (found 2026-09-20, Sprint 3).** `ci.yml` grouped runs by branch with `cancel-in-progress: true`, so the #74 merge cancelled `ActionStation CI` #681 for #75's merge commit `a2c2b99` (4 minutes earlier). The commit was still validated by its PR CI and by the deploy's own full check (Production Deploy #38 succeeded), but it has no completed post-merge CI record. **Fix in PR (not ticked until merged and seen working):** every commit on `main` gets its own concurrency group (`github.sha`); PRs and feature branches still cancel superseded runs; guarded by `ciConcurrency.structural.test.ts`. Note: changing `.github/workflows/` is not in the deploy ignore list, so merging it runs one production deploy (no behaviour change) `(Claude)` **Sprint 4 (2026-09-20):** the first merge after the fix (`480b264`) has a completed, successful `ActionStation CI` (run 35521030995) alongside its deploy; to prove the fix, two merges must land while the first one's CI is still running and both must finish (two quiet, well-spaced merges prove nothing) **Follow-up:** #77 (16:44 UTC) and #78 (17:09 UTC) also merged during Sprint 4 and both got a completed, successful CI run, but they were 25 minutes apart, so this still does not exercise the cancellation case
- [ ] C17 **`main` is not branch-protected (found 2026-09-20, Sprint 3).** `gh api .../branches/main/protection` returns 'Branch not protected': no required status checks or reviews, and every PR is opened and merged with the same account (two Claude sessions merged into `main` 4 minutes apart). It has worked because each merge waited for green CI, but nothing enforces it. Recommend requiring a PR plus the CI checks (typecheck, lint, tests, build, functions, audit, gitleaks) without required human reviews (a second reviewer cannot exist on a single account); optionally a required-reviewer gate later (see C12). Also agree one session merges at a time `(You)` decision; `(Claude)` can set it in the GitHub UI or via `gh api` on request
- [ ] C18 **Returning visitors keep running an old app version until they click "Update now" (found 2026-09-23).** `vite.config.ts` uses `registerType: 'prompt'`: a new service worker installs but stays `waiting` until the user accepts the `SwUpdatePrompt` (B14b made the check hourly and on tab focus, but acceptance is still manual). On 2026-09-23 this stranded the #84 sign-in fix: after the deploy, Chrome and Safari kept serving the old cached shell, and the auth iframe was served from `cache-storage` by the old worker, so Google sign-in stayed broken until the waiting worker was activated by hand (`postMessage({type:'SKIP_WAITING'})`). Harmless while there are no users; before M1 a critical fix must reach everyone. Recommended fix `(Claude, TDD)`: auto-activate the waiting worker on signed-out pages (landing, `/login`), where there is no unsaved canvas state, and keep the prompt for signed-in users; alternatively `registerType: 'autoUpdate'` if mid-session reloads are acceptable `(You: decide)` **Decided 2026-09-23 (owner): auto-activate on signed-out pages.** Fix in PR (`useAutoApplySwUpdate`: applies only when auth has resolved to signed out and no sign-in is in progress; 60 s loop guard; signed-in users keep the prompt). Not ticked until deployed and a signed-out tab is seen picking up the next deploy without a click
- [ ] C10 GitHub Actions Node 20 deprecation warnings — bump action runtimes
- [x] C11 `firebase-tools` pinned (broke `storage:rules` in v15) — *2026-09-20: `FIREBASE_TOOLS_VERSION: '15.30.2'` in `deploy.yml` and `ci.yml`, every `npx` invocation uses it; enforced by `src/__tests__/ciWorkflows.structural.test.ts`; PR #54 dry-run ran the pinned version. Moving off `FIREBASE_TOKEN` is tracked in C11a*
- [ ] C11a **No CI credential can deploy except your personal token (found 2026-09-20).** `FIREBASE_SERVICE_ACCOUNT` is `firebase-adminsdk-fbsvc` (roles: sdkAdminServiceAgent, appcheck.admin, auth.admin, tokenCreator, storage.admin; one long-lived key from 2026-02-28). Its role lacks `cloudfunctions.functions.list`, `datastore.indexes.*`, `firebaserules.releases.create`, `iam.serviceAccounts.actAs`, `cloudscheduler.jobs.create`, so it cannot deploy rules/indexes/functions or run a functions dry-run. CI therefore deploys with `FIREBASE_CI_TOKEN` = a personal refresh token (deprecated, full account access, and `preview.yml` already hands the adminsdk key to PR runs). Needs a dedicated least-privilege deployer identity, ideally Workload Identity Federation (no stored key) `(You)` decision — *Progress 2026-09-20: keyless Workload Identity Federation is live and proven (pool `github-actions`, provider locked to `ai-borne/actionstation`, read-only SA `ci-dryrun`, custom role `ciDryRunRulesTest` = `firebaserules.rulesets.test` only, since every predefined role with that permission can also overwrite production rules; scoped actAs on the two runtime SAs; repo vars `GCP_WIF_PROVIDER`, `GCP_DRYRUN_SA`; `scripts/setup-github-wif.sh`). **Remaining:** create a separate deploy SA whose workloadIdentityUser binding is limited to `refs/heads/main` (provider already maps `attribute.ref`), grant deploy roles (rules admin, index admin, cloudfunctions/run/scheduler/eventarc admin, secretmanager, artifactregistry, hosting), switch `deploy.yml` steps to it, remove `FIREBASE_TOKEN`/`FIREBASE_CI_TOKEN`, then revoke that token and retire the `firebase-adminsdk` key from `preview.yml` (use hosting-only SA or WIF)*
- [x] C12 Deploy runs on **every** push to `main`, including docs-only merges — add path filters or a manual approval gate before M1 — *2026-09-20: `deploy.yml` `paths-ignore` (`docs/**`, `plans/**`, `**/*.md`, `.cursor/**`, `.kilo/**`) + `workflow_dispatch` for manual deploys; structural test also asserts code paths are never ignored. Runtime proof 2026-09-20: docs-only merge `d49756c` (PR #56) created no Production Deploy run. A required-reviewer approval gate is still an option before M1 `(You)`*
- [x] C13 Add a CI step that dry-runs `firebase deploy --dry-run` on PRs so config breaks are caught before merge — *2026-09-20: job `Firebase Deploy Dry-Run` in `ci.yml` (same-repo PRs only), keyless WIF, validated rules + indexes + functions build on PR #54 (run 35485757086, attempt 4 green). Attempts 1–3 failed on missing actAs, then `firebaserules :test` permission, then IAM propagation lag, which shows the check exercises real permissions*

## D. Legal, compliance and support `M1`

- [x] D1 **Scope changed by owner decision 2026-09-22: no dedicated `support@`/`privacy@` mailboxes at `actionstation.in`.** Instead all legal/support contact now points to one already-working mailbox, `founder@ai-borne.in`, via a single SSOT: `src/config/contact.ts` `CONTACT_EMAIL`, consumed by `TermsContent.tsx` and `PrivacyContent.tsx` (commit `ba300ec`, deployed). Verified in this session: `dig MX/TXT/DMARC actionstation.in` shows no mail DNS on that domain (unchanged — expected, since mail isn't hosted there) and the site's Terms/Privacy pages resolve the contact address from that one constant. Not verified live in this session: that `founder@ai-borne.in` itself receives mail (owner states it already works). A10b's support-email note is superseded by this — the earlier blocker (`founder@ai-borne.in` not selectable in the Google OAuth console dropdown) is a separate, still-open item
- [ ] D2 Terms / Privacy reviewed by a human (entity name, address, grievance contact, India DPDP obligations; **also review the Sprint 2 additions**: Terms §5 refund clause (7-day full refund, one-time annual plan) and §7 payment-record retention on deletion, and whether the Privacy Policy must mention the retained `paymentRecords`) `(You)`
- [x] D3 Cookie banner: reject → PostHog never captures, verified in prod (plan scenario 6) — *verified 2026-09-20 in a clean browser: with no stored choice the banner shows; after **Reject** `as_analytics_consent=rejected` and PostHog's opt-out flag are set, the banner is gone after a reload, and no request to any PostHog host was made across an interaction and two page loads. Positive control: after **Accept** the page loads PostHog's `config.js` and `POST us.i.posthog.com/e/` returns 200, so the negative result is meaningful (anonymous landing page only; the logged-in path uses the same consent store)*
- [ ] D4 GDPR export and account deletion verified end-to-end in prod. **Sprint 4 (2026-09-20), partly verified:** 'Export All My Data' downloads a JSON with user, subscription, usage, calendar, `workspaces` (13 workspaces + 3 dividers), 130 nodes, 67 edges, 4 KB entries and 49 Storage files, no `data:` URIs, and matches the Settings meters. **Account deletion was not run:** the throwaway account could not sign in (Google verification), and it must never be run on the owner's main account. Deletion with a paid plan was proven in B7; a plain deletion (Firestore tree, Storage, Auth user) on a throwaway account is still to do `(You: a throwaway account that can sign in)`
- [ ] D5 `docs/compliance/PCI-SAQ-A.md` reflects Razorpay-only checkout

## E. Pre-launch scenarios — verify on production `M1`

From the commercial-readiness plan; all must pass before M1.

- [ ] E1 Pro user after Razorpay payment is **not** limited to 60 AI/day. **Sprint 4, code and tests only (no payment created):** `geminiProxy.ts:91` picks `AI_DAILY_PRO_LIMIT` (500) for `pro` and the free limit only when the plan is expired or refunded; covered by `geminiProxy.test.ts` ('applies AI_DAILY_PRO_LIMIT for pro tier users' and the expired/refunded cases). Pro is capped at 500/day, not unlimited (see I5). Not ticked: needs a live Pro account above 60 generations
- [x] E2 App Check enforced → AI generation works end-to-end — *Verified live 2026-09-20 (Sprint 3): the owner ran AI generations in the signed-in production app; `geminiproxy` logged 200 + `[SECURITY:ai_prompt] Prompt accepted` at 09:52:41 and 09:53:20 UTC (Chrome user agent), with App Check enforced (C1) and no 401s. Model served: `gemini-3.1-flash-lite-preview`, fixed server-side (`GEMINI_MODEL` in `functions/src/utils/securityConstants.ts`); the client cannot choose it (see C15)*
- [x] E3 App Check enforced → link preview works (no direct-fetch fallback in prod) — *Verified 2026-09-20 (Sprint 3): `fetchlinkmeta` returned 200 for real browser requests at 09:43 UTC and 32 × 200 in 30 days with no 401; the 400s come from URL validation (`invalidUrl` / SSRF check; App Check failure is 401), not App Check. Direct fetch runs only when `import.meta.env.DEV` (false in the production build): `linkPreviewService.ts` uses `deps.isDev` for every fallback*
- [x] E4 Workspace with 1,200 nodes → save does not delete unseen nodes. **Sprint 4, code read only:** both the load and the delete-sync in `workspaceNodeEdgePersistence.ts` use `fetchAllCollectionDocs`, which paginates past `FIRESTORE_QUERY_CAP` (1,000) and is unit-tested at the page boundary, so nodes beyond 1,000 are seen before anything is deleted. No test drives `saveNodes` itself with more than 1,000 existing docs, so this is not ticked. **Closed 2026-09-22 (TDD, unit-tested; no live 1,200-node workspace run):** new test in `workspaceService.save.test.ts` mocks `getDocs` to return two pages (1,000 + 200 docs, matching `FIRESTORE_QUERY_CAP`) and calls `saveNodes` keeping only one node from each page. Asserts: both pages are fetched before any write decision (`getDocs` called exactly twice); all 1,198 unseen nodes are deleted, including one that exists only on page 2 (`node-1050`, proving page 2 is seen before the diff, not silently dropped); the two kept nodes — including the one on page 2 (`node-1199`) — are never deleted; both kept nodes are written. This total (1,200 ops) exceeds `TRANSACTION_WRITE_LIMIT` (500), so the test also exercises the `chunkedBatchWrite` fallback path, not just the transaction path the other tests in this file cover. `npm run test` green (652 files / 17,681 tests including this one), typecheck/lint clean. **Not exercised:** a real production workspace with 1,200 live nodes (would need a seeded account) — the risk this item was tracking (silent data loss from pagination) is now covered by a regression test instead
- [x] E5 Follower tab cannot lose data without a warning — *verified live 2026-09-20: a second tab opened while the owner's tab was leader showed 'This workspace is open in another tab. Edits from this tab will not be saved.' with a 'Take over editing' button; followers do not write (`MultiTabBanner.test.tsx`, `tabLeader*.test.ts`). The take-over button itself does not work, see E5a*
- [x] E6 Turnstile with site key set → login completes — *Verified 2026-09-20 (Sprint 3) from production logs: site key is in the live bundle and `verifyturnstile` returned **200 ×5** on 2026-09-20 (`res.status(200).json({ success: true })` only on a passed challenge; a failure is 403, none logged) from the Sprint 2b sign-ins that then reached the app*
- [x] E7 Rules and functions deploy from CI on merge to `main` (pipeline, not manual) — *`deploy.yml` runs `firebase deploy --only firestore:rules,storage:rules,firestore:indexes` and `--only functions` on merge; Production Deploy run 35521030944 (merge `480b264`) succeeded with steps 'Deploy Firestore and Storage rules', 'Deploy Cloud Functions' and 'Deploy to Firebase Hosting' all green; 24 functions ACTIVE (2026-09-20). Docs-only merges skip the deploy by design (C12)*
- [ ] E5a **'Take over editing' does nothing while the other tab is alive (found 2026-09-20, Sprint 4).** `MultiTabBanner` only calls `window.location.reload()`; on reload `tabLeaderService.tryClaimOrFollow` sees a fresh leader heartbeat and makes the tab a follower again (observed live: clicked twice, banner stayed). No data is lost (the warning is shown and followers do not write), but the button promises something it cannot do; it works only after the other tab is closed. Either make take-over really claim leadership (and demote the other tab) or relabel it 'Reload' `(Claude; needs your call on behaviour)`. **Note (2026-09-20):** #77 (`4dc8315`) made a leader resign on pagehide and let followers recover from a stale heartbeat, which fixes 'closed/reloaded leader leaves everyone a follower'. The banner button on `main` still only reloads, so with a live leader it still does not take over
- [ ] E8 **Over-limit (legacy) accounts: safe but poorly explained (found 2026-09-20, Sprint 4).** Live check on the owner's Free account with more workspaces than the cap: the UpgradeWall correctly blocks a new workspace ('reached the maximum of 5 workspaces… 14/5'), nothing is written and nothing is deleted (guards only run on creation, `useWorkspaceOperations.ts:77`). Gaps: the copy does not say existing workspaces are kept, and the Settings meter prints `13/5` with a full bar. Also the sidebar per-workspace count is stale until a workspace is opened (`SecureMax (0)` really held 4 cards). Decide whether legacy accounts should get a 'grandfathered' message `(You: decide; Claude: build)`
- [x] E9 **Deleting an image Knowledge Base entry leaves the file in Storage, and the usage meter under-counts (found 2026-09-20, Sprint 4).** The upload converts the PNG to JPEG and stores `as-test-upload.jpg`, but `handleDelete` builds the path from `entry.originalFileName` (`.png`), so `deleteKBFile` targets a non-existent object and its empty `catch` hides it: the Firestore entry is deleted, the file stays (verified with `gcloud storage ls`). Related: `usage/storage` says 0 MB while the account holds 3.08 MB in 49 files (legacy files are not counted, and deletes do not lower the counter); KB entry ids are built with `Date.now()` (`kb-<epoch>-<random>`), against the ID rule in CLAUDE.md. **Fixed 2026-09-22 (TDD, code only — not yet deployed):** root cause was the upload path (`${result.title}.jpg`, from `parseResultPersister.ts`) diverging from the delete path (`entry.originalFileName`, the pre-compression name) — different filenames, so `deleteObject` silently no-opped and the Storage-delete trigger that decrements `usage/storage` never fired, explaining both symptoms as one bug. Fix: new `storedFileName` field on `KnowledgeBankEntry`/`KnowledgeBankEntryInput`, set at upload time to the exact name passed to `uploadKBFile`, persisted to Firestore, and read back by all three delete call sites (`useKnowledgeBankPanelHandlers.handleDelete`, `useDocumentGroupHandlers.handleDeleteGroup`, `knowledgeBankService.deleteAllKBEntries`), each falling back to `originalFileName` for legacy entries created before this fix (those stay unrecoverable without a migration — out of scope). No client-side counter change was needed: `usage/storage` is written server-side by the Storage `onFinalized`/`onDeleted` triggers (`storageUsageService.ts` is read-only), so a delete that actually reaches the right object now decrements it automatically. KB entry IDs switched from `kb-${Date.now()}-${random}` to `kb-${crypto.randomUUID()}` in both `knowledgeBankService.ts` and `parseResultPersister.ts` (CLAUDE.md ID rule). Tests: `parseResultPersister.test.ts` (new, 3 cases — proves the uploaded filename and `storedFileName` are literally the same value), `kbDeleteStoredFileName.test.ts` (new, 4 cases across both hooks, plus a legacy-fallback case), `knowledgeBankService.test.ts` (+4 cases: UUID-format id, `storedFileName` round-trip through `addKBEntry`/`loadKBEntries`, omitted when absent). `npm run test` green (652 files / 17,681 tests), typecheck and lint clean, `npm run build:quick` succeeds. PR #83 merged `c54cb7b`, Production Deploy run 35804012653 succeeded 2026-09-23 00:53 UTC. **Not yet proven live:** a repeat of the original repro (upload an image, delete it, confirm `gcloud storage ls` is empty and `usage/storage` drops) against the deployed app — code and CI evidence only. **Cleanup (2026-09-20):** the orphaned test object from the original repro was removed with `gcloud storage rm` at the owner's request

## F. Resilience (plan Phase 7) `M1`

- [ ] F1 500+ node workspace: performance, spatial chunking, progressive loading
- [ ] F2 Three tabs editing: no data loss
- [ ] F3 Offline → online: queued saves flush; slow 3G has no timeout crashes
- [ ] F4 Browsers: Chrome, Firefox, Safari, Edge (latest 2); Chrome Android, Safari iOS; PWA install on both
- [ ] F5 Touch on canvas: pinch zoom, drag nodes
- [ ] F6 Accessibility: Lighthouse a11y 90+ (target 95+), keyboard-only flows, VoiceOver and NVDA passes

---

## G. Gold Standard for BASB `M2`

BASB = **C**apture → **O**rganize → **D**istill → **E**xpress. A feature belongs here only if it reduces friction between thought and capture, or between capture and insight. Targets marked *(proposed)* are starting points to confirm with you.

**Capture**
- [ ] G1 Capture-to-node latency and keystroke count measured; *(proposed)* under 2 s, fully keyboard-driven
- [ ] G2 Mobile capture path decided (evaluate `docs/Editors/mobile_platforms` and `.cursor/plans/kmp_mobile_input_pipeline_*`) `(You)` decision
- [ ] G3 Workspace templates: 5 built-ins (Project Plan, Research Canvas, Brainstorm, Weekly Review, BASB CODE) — plan 8.1
- [ ] G4 Blank-canvas activation: onboarding reaches "first useful node" quickly; funnel measured (needs consent-gated PostHog)

**Organize**
- [ ] G5 Tagging, Knowledge Bank and cluster suggestions reviewed against real BASB/PARA usage in beta
- [ ] G6 Search is fast at 500+ nodes and finds what users expect

**Distill**
- [ ] G7 Synthesis output quality reviewed on real beta content; prompts tuned; AI cost per active user tracked
- [ ] G8 AI failure states are graceful (limits, timeouts, safety filter) with clear copy

**Express**
- [ ] G9 Shareable read-only canvas link with expiry — plan 8.2
- [ ] G10 Markdown/branch export verified against real content
- [ ] G11 Landing page SEO: OG image, JSON-LD, prerendering decision (plan 5.2)

**Quality bars**
- [ ] G12 Playwright E2E suite for golden paths: sign-in, capture, save/reload, AI, upgrade, export, delete
- [ ] G13 Performance budgets enforced in CI (Lighthouse perf, bundle size, boot time)
- [ ] G14 In-app feedback + "report a bug" + changelog — plan 8.3
- [ ] G15 Closed beta with 5–10 real BASB practitioners; findings triaged into this file

## H. Growth (post-launch) `M2`

- [ ] H1 Referral / invite loop — plan 8.4
- [ ] H2 Stripe / global billing and tax — only if going beyond India
- [ ] H3 Revisit Cloudflare Pages / R2 and WAF choices against real cost data

## I. Housekeeping

- [ ] I1 Decide on untracked files: `.cursor/plans/kmp_mobile_input_pipeline_*.plan.md`, `.kilo/kilo.json`, `docs/Editors/` (commit or ignore)
- [ ] I2 Delete or archive `~/Downloads/actionstation-website` (unrelated AI-BORNE fragment) `(You)`
- [ ] I3 Retire stale plan docs or mark them "superseded by this checklist"
- [ ] I4 Update `PRODUCTION-LAUNCH-PLAN.md` status table to point here
- [ ] I5 `CLAUDE.md` "Free tier limits" table says Pro is Unlimited; `tierLimits.ts` (SSOT) is 50 workspaces / 500 nodes / 500 AI per day / 5,120 MB. Update the table (it is your file, so not changed here) `(You)`
- [ ] I6 **PWA icons are a placeholder (found 2026-09-20, Sprint 3).** `public/pwa-192x192.png` and `pwa-512x512.png` are a plain solid-blue square, so an installed PWA shows no logo. Replace them with the ActionStation mark (blue circle + white check, as in `favicon.svg`; a 240 px render exists from the A10b logo). Tied to F4 (PWA install) `(Claude, needs your OK on the design)`

---

## Change log

| Date | Change |
|------|--------|
| 2026-09-23 | **Google sign-in fixed on Chrome and Safari; link-preview images fixed.** Root cause of the sign-in breakage since `5cb77a9`: with `authDomain = www.actionstation.in`, the PWA service worker's `navigateFallback` served the SPA for Firebase's `/__/auth/handler` and `/__/auth/iframe` (proven live: the auth iframe contained the app). Plus: Turnstile ran after sign-in and signed users back out when `LoginPage` unmounted. **#84** (`fbda9d4`): `navigateFallbackDenylist: [/^\/__\//]` + structural test, Turnstile verified on mount, band-aids reverted (`X-Frame-Options DENY`, `frame-ancestors 'none'`, Sentry Replay back on). **#85** (`8cbb01e`): every `proxyImage` request had returned 401 since `d6bbf18` (App Check header impossible on `<img>`) and ID tokens rode in image URLs; new `signImageUrls` callable + signed URLs only (live: `signimageurls` 200, `proxyimage` 200, image URLs carry only `url,sig,exp`). **#86** (`f98a1fc`): workspace-list load bounded at 15 s with cached fallback + Retry (Safari hang after clearing site data), Turnstile widget removed on unmount. Verified live in Chrome; Safari sign-in and workspace load confirmed by the owner. A8 ticked; new item C18 (stale service worker needs a manual "Update now") |
| 2026-09-23 | **PR #83 merged and deployed** (`c54cb7b`, Production Deploy run 35804012653 succeeded 00:53 UTC). E9 and A10e updated from "code only" to "merged and deployed"; still open for both: a live repro against the deployed app (not yet run) |
| 2026-09-22 | **D1 scope change, and E9/A10e/E4 fixed (TDD).** D1: owner decided against dedicated `support@actionstation.in` mailboxes; contact now routes through one already-working mailbox, `founder@ai-borne.in`, via the `CONTACT_EMAIL` SSOT already live in Terms/Privacy (`ba300ec`) — ticked. E9: KB image delete orphaned the Storage file and the usage meter never dropped, because the upload filename (`${title}.jpg`) and the delete-path filename (`originalFileName`) diverged; fixed with a new `storedFileName` field used consistently by upload and all three delete call sites, plus `crypto.randomUUID()` KB ids (was `Date.now()`). A10e: `calendarDeleteEvent` 500'd when the Google event was already gone; `gcalFetch` now accepts a `treatAsSuccess` status list, scoped to DELETE's 404/410 only (create/update/list still error on a real 404). E4: new pagination-boundary test proves `saveNodes` fetches all 1,200 existing docs across two pages before diffing, so nothing beyond page 1 is silently deleted. Full suite green: root 652 files / 17,681 tests, functions 49 files / 558 tests, both `npm run build` succeed. Opened as PR #83 (merged and deployed 2026-09-23, see above) |
| 2026-09-20 | **Sprint 4, production verification (owner's main account; no code changed).** Gmail: no reviewer questions for the OAuth verification. Ticked with evidence: A10d (create/update/delete 200s and the events really gone from Google Calendar), D3 (clean-browser reject/accept with a positive control), E5 (live follower banner), E7 (deploy run steps). Partly proven and left open: A7 (everything except delete account), D4 (export only), E1, E4, A8 (client unknown), C16 (one merge seen). New items: A10e (delete 500 on already-deleted event), E5a (take-over button is a reload), E8 (over-limit accounts: safe, poorly explained), E9 (KB image delete leaves the Storage file; usage meter under-counts). Owner's 'Workspaces 14/5' checked: guards block creation only, no data is lost. Test artefacts were removed afterwards (two `AS-TEST` cards deleted by the owner; the orphaned test image deleted with `gcloud storage rm`) |
| 2026-09-20 | **Process review of the #74/#75 merges.** Both were correct (green PR CI, one deploy for the code PR, none for the docs PR) but exposed C16 (post-merge CI on `main` cancelled by the next merge; fix in a PR) and C17 (`main` is not branch-protected; owner decision) |
| 2026-09-20 | **Google OAuth verification submitted.** Search Console domain verified (DNS TXT at Hostinger), branding verified and published, sensitive-scope request for `calendar.events.owned` submitted; Verification Center: 'data access is under review'. Awaiting Google. Open in the meantime: A10d (edit/delete sync), the optional support-email swap (locked during review), and reacting to reviewer emails |
| 2026-09-20 | **Demo video link entered in the Google console** (Data Access, saved and re-read; the link is kept out of this public repo). Verification Center says branding must be verified and published before the sensitive-scope verification can be requested, so the owner's next step is Search Console verification of `actionstation.in`, then Verify branding, then the scope request |
| 2026-09-20 | **Owner uploaded the demo video (unlisted; link kept out of this public repo)** and created several events on the `.owned` grant (`calendarCreateEvent` 200 ×5; one expected 401 → auto-disconnect → reconnect). Edit and delete were not exercised by any server call: the code only updates from the retry badge and deletes via the card handler. New item A10d (editing a card does not update its Google event); demo-video script corrected accordingly |
| 2026-09-20 | **A10c ticked** after the owner's live Disconnect/Connect test (card states, `disconnectcalendar` 200, `exchangecalendarcode` 200, consent text shows the `.owned` scope). Event creation on the new scope is proven (`calendarCreateEvent` 200 at 12:32:00 UTC); edit and delete are still to be shown. Demo video still parked for the owner's go-ahead |
| 2026-09-20 | **A10c built and merged (#72, `a3de80e`):** user-facing Google Calendar Connect/Disconnect in Settings → Account, TDD with MVVM (View/ViewModel/service), shared internals instead of duplicated logic, policy wording updated. Owner decisions: normal (non-destructive) confirm; on server failure stay connected and show an error. Demo video parked until the site is final: Claude tells the owner when to record |
| 2026-09-20 | **#67, #69, #70 merged.** #67 deployed (run 35507391328): C15 ticked after a live generation on `geminiproxy-00018`. #70 (`calendar.events.owned` + corrected policy wording) and #69 merged; #70's own run was superseded by run 35507740596 for #69's merge commit (`.env.example` is not in the docs-only ignore list, so #69 deployed too). Owner's live calendar test worked but hit the pre-#70 build, so `.owned` still needs a re-test |
| 2026-09-20 | **A10b follow-up.** #68 merged (`a24eb93`) and deployed. Owner asked for the code-correct scope (and found no in-app Disconnect: new item A10c): declared scope swapped to `calendar.events.owned` in the Google console, justification saved, `founder@ai-borne.in` added as developer contact; code change in PR #70. Demo video kept pending (Claude cannot record a real Google consent flow or upload to YouTube); script added to the runbook. Support email unchanged (console dropdown limitation) |
| 2026-09-20 | **Sprint 3 closeout.** #66 merged (`a4d8fd3`, no deploy: docs-only). A10 ticked after the owner's incognito sign-in. C15: owner chose `gemini-3.1-flash-lite`; PR #67 open. C2 hygiene: `localhost` removed from the Turnstile widget. A10b: Branding and the `calendar.events` scope declaration saved in the Google console (nothing submitted), PR #68 (revoke-on-disconnect/delete + Privacy Policy Limited Use section) open. New item I6 (placeholder PWA icons). No merge of #67/#68 without the owner's OK: both deploy |
| 2026-09-20 | **Sprint 3 merged.** #63 (A10 login scope, C3e, A10b plan), #64 (B16) and #65 (C14) merged by the owner's approval, one deploy per merge (#63 run 35503713329, #64 run 35504140246 succeeded; #65 run 35504638978, all succeeded first attempt). B16, C14, C2, E2, E3 ticked with live evidence; A10 ticked after the owner's incognito sign-in with a throwaway account showed only name/photo/email and no unverified-app page. Consent screen and Cloudflare widget read read-only (details in A10b and C2). New item C15 (production uses a preview Gemini model). No production setting was changed by me |
| 2026-09-20 | **Sprint 3, App Check and Turnstile (read-only, no production change).** C1 ticked: App Check has been ENFORCED on Firestore, Storage and Auth since 2026-03-29 (owner, audit log) and every function enforces it in code. E6 ticked from `verifyturnstile` logs (5 × 200). C2 partly verified (hostname allowlist needs a look in Cloudflare), E2/E3 need a live browser check. New observation: Razorpay's retries of the two B15 drill deliveries still return 400 (old signature) until about 24 h after 07:14 UTC, and will trip the `webhook_sig_failure` alert. PRs opened: #63 (A10 + C3e + A10b plan), #64 (B16), #65 (C14) |
| 2026-09-20 | **Sprint 3 started. C3e resolved: the alert emails did arrive** (ALERT 06:06:33 UTC, RESOLVED 06:11:30 UTC, inbox, Important; found by searching the mailbox for `alerting-noreply@google.com` after confirming the channel, policy and metric series were healthy). No production change. C3, C5 and the status snapshot no longer say "delivery unproven". PR #62 was already merged (`b02189b`) |
| 2026-09-20 | **Sprint 2b: payments proven end to end in test mode.** B15 fixed (webhook secret rotated to v3, #61 merged and deployed, `razorpaywebhook-00013` pins v3). Third test payment → webhook 200 → Pro (B2); full refund → Free (B6); throwaway-account delete with plan → `paymentRecords` kept, tree erased, post-delete refund 200 (B7); B4 ticked for test mode. Old secret versions v1/v2 disabled. New findings: **B16** (late captured-retry after refund re-grants Pro), **B17** (one test refund rejected by Razorpay), **A10** (sensitive Calendar scope on an unverified OAuth app shows Google's warning at every login), **C3e** (alert email never arrived), **C14** (flaky backoff test failed a deploy). The Razorpay dashboard has no webhook resend or delivery history: runbooks corrected. Still yours: B1, B3, B5, B9, B12 decision |
| 2026-09-20 | **Drill result:** payment window opens (B14 fixed) and two test payments of ₹2,999.00 were captured (`pay_…` 12:44 and 12:46 IST). Pro was NOT granted: all webhook deliveries failed `Invalid webhook signature` (new item B15). Also allowed `cdn.razorpay.com` (script) and `lumberjack.razorpay.com` (connect) and added hourly/on-visibility service-worker update checks (B14a/b) |
| 2026-09-20 | **B13 fixed** (new test key in Secret Manager, three functions redeployed, order creation returns 200). Drill then hit **B14**: Razorpay's checkout iframe (`api.razorpay.com`) is blocked by our CSP `frame-src`. Fix in PR for B14; the payment drill continues after it deploys |
| 2026-09-20 | **#58 merged and deployed** (run 35492607315; `razorpaywebhook-00011`, `createrazorpayorder-00012`). Drill attempt in your signed-in browser: Free baseline confirmed; the stale-bundle click was refused with 400 (B2c live proof); the fresh-bundle click reached Razorpay and failed `Authentication failed` because the Razorpay test key was regenerated on 18 Aug (new item B13). No order, payment or refund was made. B2/B6/B7 stay open until B13 is fixed |
| 2026-09-20 | **Razorpay dashboard checked via browser (test mode).** Added `refund.processed` to our webhook (B4). Found the account is shared with SSBMax: the 31 C3c errors were SSBMax payments, so my earlier root-cause claim for C3c/B2a was wrong and is corrected. PR #58 makes the webhook ignore foreign events quietly (otherwise every SSBMax payment would page). New item B12 (account separation decision). Dashboard is in Live view, not activated: no live keys yet |
| 2026-09-20 | **PR #56 merged: first runtime proof of C12** — no Production Deploy run was created for docs-only merge `d49756c`. **Monitoring script applied to production** (C3d): new `webhook_processing_error` metric and `HIGH: Webhook Processing Error` policy; existing policies updated in place. Still open: payment drill (B2/B4/B6/B7) |
| 2026-09-20 | **Merged and deployed #54 and #55.** Both deploy runs succeeded; site 200, health ok, 24 functions ACTIVE. Live bundle scan confirms B8 and B11 (see items). B2/B4/B6/B7 wait for the payment drill; C3d still needs `setup-monitoring-alerts.sh` run against production |
| 2026-09-20 | **Sprint 2 (PR #55, not merged):** payments hardened. Found and fixed: order payments never attributed to a user (C3c root cause, B2a), the UI ordered the ₹100 plan and any capture granted 365 days (B2c), price ₹2,999 vs ₹4,999 (B2b), server never expired Pro (B2d), refunds ignored (B6a), account deletion lost the payment trail (B7a), no alert on `webhook_processing_error` (C3d). Stripe client code removed (B8). Runbook rewritten (B10), copy aligned (B11). Open for you: live drill (B2/B6/B7), Razorpay dashboard webhook check (B4), KYC/live keys/GST (B1, B3, B5, B9), apply monitoring script (C3d) |
| 2026-09-20 | **Sprint 1 (PR #54, not merged):** C3 monitoring fixed (alerts were dead on gen2), C4 backups repaired (had 403'd since setup; restore drill passed), C5 uptime checks live, C11 pinned, C12 paths-ignore, C13 WIF dry-run. New blockers found and logged: C3a–c, C4a–h, C11a. Open for you: C4e PITR decision, C4g retention lock (irreversible), C4h legacy bucket, C11a deploy identity |
| 2026-09-20 | PR #51 and #52 merged; production deployed and verified (A4, A5, A6, A9). Deploy exposed CI tooling gaps (C11–C13). Fixed flaky `withRetry` backoff test. Lesson: run the **full** `npm run check` before pushing, not just structural tests |
| 2026-09-19 | Created. Domain connected (`www.actionstation.in`), SEO aligned, CI fixed, PR #51 green. Live-state audit: Razorpay test keys, no WAF, 2 alert policies, no uptime monitor, no mailboxes |
